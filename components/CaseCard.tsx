import { CaseItem } from '@/data/cases';
const badgeStyles: Record<CaseItem['status'], string> = { ongoing:'bg-yellow-100 text-yellow-800', settled:'bg-green-100 text-green-800', dismissed:'bg-gray-200 text-gray-800', appeal:'bg-blue-100 text-blue-800', legislation:'bg-purple-100 text-purple-800' };
export function CaseCard({ item }: { item: CaseItem }) { return <article className="rounded-xl border border-gray-200 p-5"><div className="flex items-start justify-between gap-3"><h3 className="text-lg font-semibold">{item.caseName}</h3><span className={`rounded-full px-2 py-1 text-xs font-medium ${badgeStyles[item.status]}`}>{item.status}</span></div><p className="mt-1 text-sm text-gray-600">{item.parties}</p><p className="mt-1 text-xs text-gray-500">{item.country} · {item.sector} · {item.date} · Last updated {item.updated}</p><p className="mt-3 text-sm text-gray-700">{item.explanation}</p><div className="mt-3 flex flex-wrap gap-2">{item.tags.map(t=><span key={t} className="rounded-full border border-gray-300 px-2 py-0.5 text-xs">{t}</span>)}</div></article>; }
